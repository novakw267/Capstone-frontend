<h2>Title: {{blog.title}}</h2>
<h3>Subject: {{blog.subject}}</h3>
<h3>Blog: {{blog.body}}</h3>
<h3>Who is starting stuff?: {{blog.author}}</h3>
{{#link-to "comment.index" class='comment btn btn-success'}}View Comments{{/link-to}}
<button class='comment btn btn-info' {{action "new"}}>Add Comment</button>
{{#if blog.editable}}
<button class="btn btn-warning" {{action 'edit'}}>Edit Blog</button>
<button class='delete btn btn-danger' type='submit' {{action 'delete'}}>Delete Blog</button>
{{/if}}
