<h2> Your thoughts? </h2>
<form>
<div class="form-group">
  <label>Who is talking to me?</label>
  {{input type="text"
          class="form-control"
          maxlength="50"
          placeholder="Author"
          value=comment.author}}
</div>

<div class="form-group">
  <label>Thoughts?</label>
  {{input type="text"
          class="form-control"
          maxlength="75"
          placeholder="Subject"
          value=comment.idea}}
</div>
<button type="submit" class="btn btn-success" {{action 'save'}}>
  Create
</button>

{{#link-to "comment.index" class="btn btn-primary"}}Go Back{{/link-to}}
</form>
